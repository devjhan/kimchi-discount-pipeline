# ============================================================
# requirements.txt — investment_v3 runtime dependencies
# ============================================================
# pip 으로 설치되는 third-party Python 의존성 SSoT.
# scripts/bootstrap.sh 가 .venv/ 안에 본 파일을 install 한다.
# 모든 entry point (hook / orchestrator) 는 infrastructure/_common/python_env.sh
# 의 resolve_python() 으로 .venv/bin/python3 를 우선 채택한다.
# ============================================================

# YAML parsing — governance/*.yaml (topology / thresholds / schedules / ...)
# 의 SSoT 파일 로드에 필수. 미설치 시 doctrine_lint.py 가 silent skip → 모든
# governance 정합성 검증이 무력화된다.
pyyaml>=6.0
