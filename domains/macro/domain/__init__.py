"""domains/macro/domain — 도메인 frozen value objects.

- ``regime.py`` `IndicatorResult` — 단일 indicator 의 (값, label, citation, skip)
- ``regime.py`` `RegimeResult` — classify_regime 의 산출 (regime / rationale / votes)
"""
from __future__ import annotations
