"""domains/universe/domain — 도메인 핵심 frozen 객체 (entry / result).

Run 1: ``UniverseEntry`` (단일 종목 후보 + metadata).
Run 4: ``UniverseResult`` (build_universe 출력 묶음).
Run 5: ``EnrichedEntry`` (entry + attached attribute Mapping).
"""
from __future__ import annotations
