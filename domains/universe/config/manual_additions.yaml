# ============================================================
# manual_additions.yaml — User Manual Universe Additions (Run 2)
# ============================================================
# LiteralListSource (sources.yaml 의 type=literal_list entry) 가 본 파일을
# items_ref 로 읽음.
#
# 원본: governance/thresholds.yaml.universe.manual_additions (Run 2 시점 [] 빈 리스트)
# Run 4 deprecate 후 본 파일이 canonical, thresholds.yaml 의 manual_additions 키
# 는 제거 예정 (parity validation 후).
# ============================================================

schema: "universe-manual-additions-v1"
version: 1
description: "사용자 명시 universe 추가 종목. 각 entry 는 str (ticker only) 또는 dict (ticker / name / reason)."
last_updated: "2026-05-17"

# entry 형식 예:
#   - "KR:003550"
#   - {ticker: "KR:003550", name: "(주)LG", reason: "지주사 NAV 할인 진입 후보"}
items: []
