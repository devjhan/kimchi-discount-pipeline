# ============================================================
# exclusions.yaml — User Manual Universe Exclusions (Run 4)
# ============================================================
# build_universe (application/build_universe.py) 가 fan-in discovery 후
# 본 파일의 ``tickers:`` set 을 이용해 dedup 결과에서 ticker 단위 제외.
#
# 원본: governance/thresholds.yaml.universe.exclusions (Run 4 시점 [] 빈 리스트)
# Run 4 deprecate 후 본 파일이 canonical, thresholds.yaml 의 exclusions 키는
# 제거 (legacy alpha_factory/universe.py 삭제 PR 와 동일 commit).
# ============================================================

schema: "universe-exclusions-v1"
version: 1
description: "사용자 명시 universe 제외 ticker. fan-in 후 ticker 단위 set membership filter."
last_updated: "2026-05-17"

# 예: tickers: ["KR:000999"]
tickers: []
