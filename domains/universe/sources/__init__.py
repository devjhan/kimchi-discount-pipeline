"""domains/universe/sources — DiscoverySource plugin 디렉토리.

Run 1: stub (placeholder).
Run 2: ``base.py`` (DiscoverySource ABC) + ``literal_list.py`` +
       ``holding_company.py`` + ``registry.py`` + ``factory.py``.
Run 3: ``dart_disclosure_filter.py`` — treasury / spin_off / activist 3 함수의
       단일 통합 구현 (Pain 1 / 2 해결).
Run 5: ``preferred_share_pair_seed.py`` — pref spread enricher 의 entries 발견.
"""
from __future__ import annotations
